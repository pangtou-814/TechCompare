{
  "name": "ft-process-explorer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "data": "node scripts/build-data.mjs",
    "dev": "npm run data && vite --host 0.0.0.0",
    "build": "npm run data && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "echarts": "^6.0.0",
    "lucide-react": "^0.468.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
